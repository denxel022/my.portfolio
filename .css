* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(45deg, #f0f4f8, #ffdde1);
    color: #333;
    line-height: 1.6;
  }
  
  header, footer {
    background: #333;
    color: #fff;
    padding: 20px;
    text-align: center;
    text-shadow: 2px 2px 5px rgba(116, 144, 33, 0.4);

  }
  
  header a, footer a {
    color: #fff;
    text-decoration: none;
    margin: 0 15px;
  }
  
  header nav a:hover {
    text-decoration: underline;
  }
  
  
  .container {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 20px;
    padding: 20px;
  }
  
  section {
    padding: 20px;
    text-align: center;
  }
  

  .home, .about {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    text-align: center;
    text-shadow: 4px 4px 10px rgba(0, 0, 0, 0.8); /* Explicit Text shadow for About Me title */
  }
  
  .user-info img {
    width: 100%;
    max-width: 150px; /* Maximum width to maintain the desired size */
    height: auto; /* Automatically adjust height to maintain aspect ratio */
    border-radius: 50%; /* Keep the round shape */
    cursor: pointer;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    transition: transform 0.3s ease; /* Add smooth transition on hover */
  }
  
  .user-info img:hover {
    transform: scale(1.1); /* Slight zoom effect when hovered */

  }
  
  
  
.modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    justify-content: center;
    align-items: center;
  }
  
  .modal-content {
    background: #fff;
    padding: 20px;
    border-radius: 10px;
    text-align: center;
    box-shadow: 0 8px 10px rgba(0, 0, 0, 0.2); /* Box shadow on modal */
  }
  
  .modal .btn {
    margin: 10px;
    padding: 10px 20px;
    cursor: pointer;
  }
  
  .modal .btn-confirm {
    background-color: #28a745;
  color: white;
  }
  
  .modal .btn-cancel {
    background-color: #dc3545;
    color: white;
  }
  
 
  .contact-form {
    background: #fff;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 8px 10px rgba(0, 0, 0, 0.1); 
  }
  
  .contact-form .form-group {
    margin: 10px 0;
  }
  
  .contact-form input, .contact-form textarea {
    width: 100%;
    padding: 10px;
    border-radius: 5px;
    border: 1px solid #ccc;
  }
  
  .contact-form button {
    background-color: #333;
    color: #fff;
    padding: 10px 20px;
    border: none;
    cursor: pointer;
  }
  
  .contact-form button:hover {
    background-color: #555;
    box-shadow: 0 8px 10px rgba(0, 0, 0, 0.3); /* Box shadow on button hover */
  }
  

  .icon {
    font-size: 3rem;
    margin: 20px;
    color: #333;
    text-shadow: 8px 8px 6px rgba(51, 20, 231, 0.5); /* Explicit Text shadow on icons */
  }
  
  /* Animated Background */
  body {
    animation: backgroundMove 10s ease-in-out infinite;
  }
  
  @keyframes backgroundMove {
    0% {
      background-position: 0 0;
    }
  
    100% {
      background-position: 100% 100%;
    }
  }
  
  /* Media Queries */
  @media (max-width: 768px) {
    .container {
      grid-template-columns: 1fr;
    }
  }
  